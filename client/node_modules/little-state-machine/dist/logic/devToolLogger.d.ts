import { GetStore, Store } from '../types';
export declare function logStartAction({ debugName, getStore, }: {
    debugName: string;
    getStore: GetStore;
}): any;
export declare function logEndAction({ getStore, storeCopy, }: {
    getStore: GetStore;
    storeCopy: Store;
}): void;
